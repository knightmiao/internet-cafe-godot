# 缺失素材清单 v1

> 对应 640×360 逻辑画布。尺寸均为逻辑像素，素材按 1:1 绘制。
> 当前 Godot 界面均已用色块、文字或符号占位，替换素材时不需要重做布局。

## P0：搭建真实游戏场景前必须补

### 地图 TileSet

- [x] `assets/world/tiles/floor_atlas.png`：32×32 双色地板 + 卫生间湿区
- [x] `assets/world/tiles/wall.png` / `partition.png` / `fence.png`：16px 子网格墙体、隔断与围挡
- [x] `assets/world/facilities/entrance.png`：入口
- [x] `assets/world/facilities/counter.png`：96×40 收银柜台
- [x] `assets/world/facilities/shelf.png`：64×40 小卖部货架
- [x] `assets/world/facilities/toilet.png`：64×80 卫生间
- [x] `assets/world/stations/desk_left/mid/right.png`：连续联排桌组件
- [x] `assets/world/stations/pc_station.png`：显示器、主机、键盘与椅子

当前状态：已接入 `TileMapLayer` 与独立设施节点。复杂物件走生图像素化管线，
无缝 tile 与反馈图标由 `godot/tools/gen_stage_tiles.py` 精确生成。

### 角色 Sprite

- [ ] `boss_walk.png`：老板四向行走，每向 4 帧
- [ ] `assets/world/npc/customer_01～50_walk.png`：50 套顾客四向行走（本轮明确不做）
- [x] `assets/world/npc/customer_01～50_idle.png`：50 套顾客经营态立姿，32×72
- [ ] 顾客坐姿：需按 50 套外观补齐；当前机位只显示设备

当前状态：50 人固定角色库已包含姓名、年龄、职业、着装、身高、发型发色及上网
习惯；场景中的 3 名顾客从角色库抽取，并使用独立像素外观与状态气泡。

### 右栏特写

- `portrait_counter.png`：144×96，柜台默认态，老板 + 布偶猫 + 收银机（已完成并接入）
- `portrait_pc.png`：144×96，机位特写，电脑 + 座椅 + 顾客
- `portrait_customer_01.png`：64×96，顾客半身像
- `portrait_customer_02.png`：64×96
- `portrait_customer_03.png`：64×96
- 心情小图标：满意 / 普通 / 不满，各 12×12

当前状态：柜台已接入真实素材，机位与顾客仍为右栏居中文字说明。

**特写素材工作流**（`portrait_counter` 已验证，其余特写沿用）

1. 生图模型出概念稿，存入 `docs/ui/concept/`。提示词要求横构图、大像素块、限定色数、无文字，并写明角色形象设定
2. `godot/tools/pixelize_portrait.py` 裁掉外框、居中裁成目标比例、区域平均降采样、量化到 32 色
3. `godot/tools/gen_counter_portrait.py` 按坐标重画眼镜、眼睛、鼻嘴和猫的蓝眼。降采样会把这些高对比细节抹平，必须手工补
4. 验收看 2x（游戏内实际显示尺寸），不要放大到 8x 以上抠细节，会过度纠结

新增 PNG 后需执行 `godot --headless --path godot --import`，否则 `load()` 会报 no loader found。

## P1：UI 像素风统一所需

### 顶栏

- `brand_maowang.png`：建议 56×20，「猫网电竞」像素字标
- 指标图标各 12×12：机位、金钱、顾客、声誉、清洁、装修
- 系统图标各 12×12：消息铃、消息铃红点、设置

当前占位：文字「机 / ¥ / 客 / 星 / 洁 / 装 / ! / ⚙」。

### 面板与按钮

右栏操作区已完成，走复古电脑风（米白塑料机身 + CRT 特写屏 + 琥珀概览屏 + 凸起键帽），素材由 `godot/tools/gen_panel_theme.py` 程序化生成到 `godot/assets/ui/panel/`：

- [x] `panel_case.png` 16²，margin 4：机身外壳，四角螺丝
- [x] `screen_frame.png` 10²，margin 3：特写 CRT 框
- [x] `amber_frame.png` 10²，margin 3：概览琥珀屏框
- [x] `key_normal / key_hover / key_pressed / key_disabled.png` 12²，margin 4：上下文键帽四态
- [x] `modkey_normal / modkey_hover / modkey_pressed / modkey_disabled.png` 8²，margin 2：模块与走带小键四态

### 动作 icon

由 `godot/tools/gen_action_icons.py` 生成到 `godot/assets/ui/icons/`，纯白剪影靠 Godot 主题色调制，一套同时服务可点与禁用态：

- [x] 动作 icon 10×10 共 17 个：`shop_open` `shop_close` `serve` `restock` `cash` `power` `clean` `repair` `details` `member` `remove` `inventory` `pricing` `promotion` `preview` `plan` `lock`
- [x] 走带 icon 8×8 共 3 个：`tp_pause` `tp_play` `tp_fast`

仍待补：

- 模块图标 16×16 共 7 个（见下节），到位后模块小键才能从纯文字改成 icon
- `button_danger.9.png`：危险操作按钮（劝离、解雇等），需要一套红键帽
- `selection_frame.9.png`：场景对象选中框
- `badge_dot.png`：3×3 或 5×5 红点

左侧场景内的按钮（机位、顾客、柜台、货架）仍是 `StyleBoxFlat` 色块占位，等 TileSet 与 Sprite 到位后一并替换，不复用键帽样式。

### 模块图标

每个 16×16：财务、人力、机器、采购、事件、档案、升级，共 7 个。第 8 格留空；设置使用顶栏已有的 12×12 齿轮，不重复制作模块入口。图标到位后模块格可从 33 × 24 压到 24 × 24，右栏还有一次收窄到 128 的余地。

当前占位：纯文字键帽。33px 宽塞不下 10px icon 加两个 8px 字，所以模块小键暂不跟上下文键帽一样上 icon，等这批 16×16 到位再统一改。

### 状态与气泡

- [x] 机位状态点各 6×6：已关机灰、空闲绿、使用中蓝、待清洁黄、故障红
- [x] 顾客气泡各 16×16：等待、结账、点单、不满
- [x] 32×32 场景对象像素选中框
- [ ] 特殊事件气泡

### 装修槽位

- [x] 青色虚线空槽、绿植、垃圾桶、海报、灯牌、空调首批预览素材
- [x] 大厅、入口、柜台、小卖部、包间、卫生间、高配区共 12 个预设槽位
- [x] 经营态隐藏空槽，装修预览态显示；已安装装修始终显示
- [ ] 墙纸/地板完整区域皮肤与升级数值

## P2：内容扩展时补

- 机器升级外观：低配 / 中配 / 高配 / 电竞机
- 桌椅、装饰、绿植、空调、灯牌等装修物件
- 小卖部商品图标：饮料、泡面、零食、香烟等
- 事件插图：256×96
- 财务、人力、采购、档案等弹窗插图与空状态
- 地图扩建区域、楼梯、二层、包间主题套装

## 字体

- 需要引入 Zpix 或 Fusion Pixel 12px 简体中文字体
- 正文与按钮：12px
- 顶栏受空间限制可使用 8～10px 的配套小号点阵字
- 当前使用系统字体，仅用于布局阶段

## 导入规则

- PNG 使用无损导出，透明背景
- Godot 纹理过滤关闭，禁止 mipmap
- 像素素材不在引擎内做非整数缩放
- Sprite sheet 帧之间不留半透明像素
- 全项目调色板控制在 32 色左右
